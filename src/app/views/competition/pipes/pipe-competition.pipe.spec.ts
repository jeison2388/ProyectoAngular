import { PipeCompetitionPipe } from './pipe-competition.pipe';

describe('PipeCompetitionPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCompetitionPipe();
    expect(pipe).toBeTruthy();
  });
});
